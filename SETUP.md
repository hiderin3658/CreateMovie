# セットアップガイド

## 📦 インストール手順

### 1. 依存関係のインストール

```bash
cd ai-video-storyboard
pip install -r requirements.txt
```

これで以下がインストールされます：
- `google-generativeai` - Gemini API
- `pillow` - 画像処理
- `numpy` - 配列操作
- `python-dotenv` - **.envファイルサポート**
- `scikit-learn` - 色抽出
- `scipy` - 画像処理

### 2. APIキーの設定

#### 方法1: .envファイル（推奨）🌟

1. **サンプルファイルをコピー**

```bash
cp .env.example .env
```

2. **.envファイルを編集**

```bash
# エディタで開く
nano .env
# または
code .env
```

3. **APIキーを記入**

```bash
# .env
GEMINI_API_KEY=あなたの実際のAPIキーをここに入力
```

4. **完了！**

これで毎回`export`しなくても、スクリプトが自動的に`.env`ファイルからAPIキーを読み込みます。

#### 方法2: 環境変数（一時的）

ターミナルセッションごとに設定:

```bash
export GEMINI_API_KEY='your-api-key-here'
```

#### 方法3: シェル設定ファイル（永続的）

```bash
# ~/.zshrc または ~/.bashrc に追加
echo 'export GEMINI_API_KEY="your-api-key-here"' >> ~/.zshrc
source ~/.zshrc
```

### 3. APIキーの取得

[Google AI Studio](https://makersuite.google.com/app/apikey)から無料でAPIキーを取得できます。

## ✅ セットアップ確認

### テスト実行

```bash
python scripts/generate_storyboard.py --help
```

エラーが出なければセットアップ成功です！

### 簡単なテスト

```bash
python scripts/generate_storyboard.py "テスト動画" --no-images
```

画像生成なしで絵コンテ構造だけ生成してテストします。

## 🔐 セキュリティ注意事項

### .envファイルは絶対にGitにコミットしない！

`.gitignore`ファイルに`.env`が含まれているので、通常は自動的に除外されます。

確認方法:

```bash
git status
```

`.env`が表示されなければOKです。

### APIキーの保護

- ❌ コードに直接書かない
- ❌ GitHubなどに公開しない
- ✅ `.env`ファイルを使う
- ✅ `.gitignore`で除外

## 📁 ファイル構造

セットアップ後:

```
ai-video-storyboard/
├── .env                    # あなたのAPIキー（Git除外）
├── .env.example            # サンプルファイル
├── .gitignore              # Git除外設定
├── requirements.txt        # Python依存関係
└── scripts/
    ├── generate_storyboard.py
    ├── visual_reference_analyzer.py
    └── music_generator_suno.py
```

## 🚀 次のステップ

セットアップが完了したら：

1. **[HOW_TO_USE_ja.md](HOW_TO_USE_ja.md)** - 使い方ガイド
2. **[QUICKSTART_ja.md](QUICKSTART_ja.md)** - クイックスタート
3. **[SKILL_ja.md](SKILL_ja.md)** - 完全なドキュメント

## 💡 よくある質問

### Q: `.env`ファイルが読み込まれない

**A:** `python-dotenv`がインストールされているか確認:

```bash
pip install python-dotenv
```

### Q: APIキーエラーが出る

**A:** 以下を確認:

1. `.env`ファイルが存在するか
2. APIキーが正しく記入されているか
3. ファイル名が`.env`（ドットで始まる）か

```bash
# .envファイルを確認
cat .env
```

### Q: 複数のプロジェクトで同じAPIキーを使いたい

**A:** シェル設定ファイルに追加:

```bash
echo 'export GEMINI_API_KEY="your-key"' >> ~/.zshrc
source ~/.zshrc
```

## 🔄 アップデート

依存関係を更新:

```bash
pip install --upgrade -r requirements.txt
```

---

これでセットアップ完了です！ 🎉

次は[HOW_TO_USE_ja.md](HOW_TO_USE_ja.md)で使い方を確認してください。
